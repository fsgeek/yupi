# Corrected-Kernel Rerun — Drift Report (v0.1.1)

> **Erratum v0.1.1 (2026-08-20, same day; truthsayer round two via
> Codex/ChatGPT through Tony, four findings, all verified and adopted).**
> (1) The drift table was produced by an uncommitted scratch script whose
> regex leaf-filter silently excluded `resolved_mass` and the
> per-endpoint Q4 gaps — cells were understated ((14,4,2) query
> 0.0137→**0.0339**; per-endpoint Q4 gaps 0.0069/0.0087). Replaced with
> the output of the committed `scripts/corrected_drift_table.py` (all
> shared numeric leaves; policy documented there). (2) "20 raws" was a
> miscount: **19**. (3) The "Not rerun here" section proposed stamping
> thresholds first and letting the sweep reruns double as confirmation —
> wrong sequencing, corrected below: thresholds selected from
> buggy-kernel sweeps cannot be stamped and then "confirmed" by the
> ruler they justified. (4) Fixed at the code level this round:
> statutory I2 (mutual exclusion) and I6 (blocked-cycle detection) were
> weaker than their names claimed — a two-lock cycle and a
> RUNNING-while-queued state both passed; and the program validator is
> now enforced at the kernel boundary (the kernel executed
> `(ACQUIRE(0),)` before). Suite 113 green.

**Status: measured note, written 2026-08-20 13:57 PDT (`date` in the writing command),
ninth instance.** First rerun of the C1 measurement chain under the
corrected kernel (handoff fix d69fa87, guards 87ed0a7), per the
adjudication note's revised ordering. 19 new raws
`docs/c1-*-corrected-2026-08-20.json`; every day-5–7 raw untouched.
Thresholds remain unstamped: these runs are **corrected-kernel
exploratory**. Scripts unchanged except one repair below.

## The gate that caught its own reference

The first chain run FAILED at `c1_divergent_resolution.py`: its
state-level exact-equality gate (v0.1.2, hardened after the second
truthsayer round) compared the corrected kernel against the day-seven
full-context reference — a buggy-kernel artifact — and refused. That is
the gate doing its job: it detected a law change between reference and
measurement. Repaired (v0.1.3) by pointing the reference at the
corrected-kernel full-context raw produced earlier in the same chain;
8/8 exact-equality assertions pass. The failure and repair are part of
the record on purpose.

## Drift: buggy law → corrected law (v0.1.1 — regenerated by `scripts/corrected_drift_table.py`, all shared numeric leaves)

Quantity = leaf with |old| < 10; count = |old| >= 10 (heuristic documented
in the script; small integer class-counts land in the quantity column —
predictive (14,2,2)'s "2.0" is separated-pair counts 5→3, not bits).

| note | max quantity drift | where | max count drift |
|---|---|---|---|
| c1-divergent-grid-12-2-2 | 0.00024 | rows[15].div_mass 0.0137→0.0134 | 6 |
| c1-divergent-grid-14-2-2 | 0.00107 | rows[9].div_mass 0.0652→0.0663 | 32 |
| c1-divergent-grid-14-4-2 | 0.04110 | rows[9].div_mass 0.4114→0.3703 | 8044 |
| c1-divergent-resolution-12-2-2 | 0.00370 | rows[1].state_div_mass 0.9773→0.9736 | 36 |
| c1-offset-vs-state-12-2-2 | 0.00321 | rows[1].H_S_given_U 1.5944→1.5911 | 6 |
| c1-offset-vs-state-14-4-2 | 0.00455 | rows[3].H_U 0.7258→0.7304 | 89 |
| c1-offset-vs-state-14-8-2 | 0.00272 | rows[0].frac_state_given_pos 0.1621→0.1649 | 302 |
| c1-predictive-targets-12-12-2 | 0.00370 | rows[1].divergent.mass 0.9773→0.9736 | 258804 |
| c1-predictive-targets-12-2-2 | 0.00270 | rows[1].means.kinds2.gap 0.7887→0.7860 | 6 |
| c1-predictive-targets-14-2-2 | 2.00000 | rows[1].divergent.separated_by.kinds2 5.0000→3.0000 | 42 |
| c1-predictive-targets-14-4-2 | 0.04095 | rows[2].divergent.mass 0.3952→0.3543 | 7259 |
| c1-q4-ceilings-12-12-2-W4 | 0.00010 | rows[3].total_bits 0.6676→0.6677 | 109 |
| c1-q4-ceilings-12-2-2-W4 | 0.00050 | rows[2].total_bits 0.9325→0.9330 | 109 |
| c1-q4-ceilings-14-2-2-W4 | 0.00466 | rows[1].total_bits 1.0774→1.0820 | 338 |
| c1-q4-ceilings-14-4-2-W4 | 0.00437 | rows[0].total_bits 0.8737→0.8780 | 338 |
| c1-query-ceilings-12-2-2 | 0.00366 | rows[1].queries.Q1[L0].mean_bits 0.6645→0.6608 | 6 |
| c1-query-ceilings-14-2-2 | 0.02653 | rows[6].queries.Q4proxy[L0].resolved_mass 0.3300→0.3565 | 29 |
| c1-query-ceilings-14-4-2 | 0.03394 | rows[4].queries.Q1[L0].resolved_mass 0.7393→0.7733 | 89 |
| c1-support-exact | 0.00099 | rows[0].mean_support 1.6097→1.6107 | 10 |

Count-level scars of the bug: −6 windows at (12,2,2) r3/r4; horizon-8
endpoint census 1244→1135 states (the 109 removed are exactly the
contaminated set); state P-next classes 353→317.

## Qualitative claims re-verified on the corrected raws

- **Divergent-grid structure holds**: observer-monotonicity 24/24
  columns, pair-set nesting 72/72 (asserted in-run), r4 > r1 in all six
  diagonal cells, and the diagonal r3→r4 sign pattern (− at (12,2,2),
  + at the T_ep=14 laws) all survive.
- **Divergent class non-empty at every law/rung/ε** (deliverable 5.3's
  existence answer stands).
- **Conservation identities** (irreducible-term rung-invariance) held
  in-run via the scripts' own checks; offset-is-a-coordinate-of-state
  (I(U;S)=H(U)) reproduced at all three offset-vs-state laws.
- **Magnitudes to re-quote**: (14,4,2) divergent masses are ~10%
  (relative) lower than the day-seven values; the (14,·) endpoint-14
  entropies move in the second decimal. Any prose citing those specific
  numbers must cite the corrected raws from now on.

## Not rerun here — and the stamp sequencing (v0.1.1, corrected)

Sync sweep (`c1_sync_sweep.py` and per-endpoint family), δ/δ_sync/TV
sweeps, multi-waiter census, rung-gap decompositions, window-prior and
clock experiments are not in this batch. ~~The sweeps should be rerun
once, under whatever thresholds the pending second stamp fixes, to
double as the confirmatory pass~~ *(v0.1.1: wrong order — v0.3.1's
thresholds were selected from buggy-kernel sweeps; an invalidated sweep
cannot justify the ruler used to confirm its replacement).* The
defensible order, adopted: (1) rerun the three §C sweeps and the sync
sweep under the corrected kernel as **exploratory**; (2) reconsider or
reaffirm the v0.3.1 proposal against the corrected curves; (3) stamp;
(4) confirm on fresh held-out laws/configurations. The second stamp
should not be authorized before step 2's comparison is on the record.

## Caveats

- Same scripts as the originals (modulo the v0.1.3 reference repair);
  single-path aggregation over two-path-gated posteriors, as before.
- σ/entity naming remains unimplemented: all quantities are the
  role-known structural quotient (audit finding 3).
- Drift maxima are per-leaf maxima of |old − new| over leaves matching
  entropy/mass/gap patterns; full leaf-level diffs reproducible from the
  committed raw pairs.
